#!/bin/bash

set -e

case "$1" in
	upgrade|full-upgrade|dist-upgrade|downgrade|abort-upgrade|remove|autoremove|purge)
	rm -rf /etc/skel/{.config,.face,.face.icon,.bashrc,.zshrc,.local,.kde,.gtkrc-2.0,Desktop,.conkyrc,.oh-my-zsh} 2> /dev/null || true
;;
esac

#DEBHELPER#
