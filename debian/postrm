#!/bin/bash

set -e

case "$1" in
	purge|upgrade|full-upgrade|dist-upgrade|downgrade|abort-upgrade)

rm -rf /etc/skel || true
mkdir -p /etc/skel || true
rm -rf /root/.oh-my-zsh || true

;;
esac

#DEBHELPER#
